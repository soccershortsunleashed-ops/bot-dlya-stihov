Задание для IDE: уменьшить время показа toast + добавить “smoke disappear” анимацию
1) Изменить логику showToast() (layout.html)

Файл: layout.html
Место: функция showToast() 

layout

Что сделать:

Задать явные опции Bootstrap Toast:

delay: 2500 (или 2000–3000 — целевое значение)

autohide: true

Подключить “smoke-out” анимацию на скрытие:

при событии hide.bs.toast отменять стандартное скрытие (preventDefault)

запускать CSS-анимацию “дым/испарение”

после завершения анимации — вызывать toast.hide() повторно (но уже без перехвата), чтобы Bootstrap корректно убрал toast

Требование:

обработчик событий должен навешиваться один раз, а не при каждом вызове showToast() (чтобы не плодить слушатели)

Пример ожидаемой правки (смысл):

// 1) toast = bootstrap.Toast.getOrCreateInstance(toastEl, { delay: 2500, autohide: true });

// 2) один раз навесить hide.bs.toast:
// - если data-smoke-hiding = "1" -> пропустить
// - иначе preventDefault, добавить класс .toast-smoke-out
// - через ~650ms снять класс, выставить data-smoke-hiding="1", вызвать toast.hide()

2) Добавить CSS-анимацию “испарение дыма”

Файл: /static/admin.css (он уже подключён в layout) 

layout


(или временно вставить <style> в layout.html, но правильнее — в admin.css)

Что добавить:

класс .toast-smoke-out для самого toast: плавное “поднятие вверх + растворение + blur”

псевдо-элемент ::after (или ::before) с лёгкими “облаками” через radial-gradient, которые поднимаются и исчезают

Цель по ощущениям:

длительность анимации: 500–800ms

выглядит как “пшик дыма” и затем исчезновение

Минимальные требования к анимации:

исчезновение не резкое

заметен лёгкий эффект дыма (градиенты/blur/сдвиг вверх)

Acceptance Criteria (готово, когда)

После сохранения toast “Успех” показывается ≈2–3 секунды, затем исчезает сам (без клика).

Исчезновение происходит с анимацией “дым/испарение” (не просто fade).

Не появляется “дёрганье”/двойное скрытие/мигание.

Поведение единое для всех мест, где вызывается showToast() (централизованно, в layout.html). 

layout

Рекомендуемые параметры (можно сразу зафиксировать)

delay: 2500ms

smoke-анимация: 650ms