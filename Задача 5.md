Ниже даю реально рабочий патч для admin.css, чтобы панель стала в стиле как на твоём примере: белый фон, чёрный/тёмный шрифт, пастельные карточки, плюс адекватные таблицы/формы. Патч сделан так, чтобы:

по умолчанию тема стала светлой (white + pastel),

при желании можно вернуть тёмную: просто повесить на <html> или <body> класс theme-dark.

Основано на текущем admin.css 

admin

1) ПАТЧ (unified diff) — переключаем на светлую тему + пастель

Применение: git apply или вручную заменить соответствующие блоки.

--- a/admin.css
+++ b/admin.css
@@ -1,22 +1,87 @@
 /* Admin SaaS Design System - Dark Theme */
-:root {
-    --bg-dark: #0f111a;
-    --card-bg: #1a1d29;
-    --border-color: #2d3245;
-    --text-primary: #e2e8f0;
-    --text-secondary: #94a3b8;
-    --accent-primary: #3b82f6;
-    --accent-success: #10b981;
-    --accent-warning: #f59e0b;
-    --accent-danger: #ef4444;
-    --sidebar-bg: #1a1d29;
-}
+/* Default theme = LIGHT (white background + pastel cards) */
+:root {
+    /* Surfaces */
+    --bg: #f6f7fb;
+    --card-bg: #ffffff;
+    --card-bg-pastel: #f2f6ff;          /* pastel tint for cards/sections */
+    --border-color: rgba(15, 23, 42, 0.12);
+    --sidebar-bg: #ffffff;
+
+    /* Text */
+    --text-primary: rgba(15, 23, 42, 0.92);
+    --text-secondary: rgba(15, 23, 42, 0.66);
+    --text-dim: rgba(15, 23, 42, 0.46);
+
+    /* Accents */
+    --accent-primary: #2563eb;
+    --accent-success: #16a34a;
+    --accent-warning: #d97706;
+    --accent-danger: #dc2626;
+
+    /* Hovers / headers */
+    --surface-header: rgba(37, 99, 235, 0.06);
+    --surface-hover: rgba(15, 23, 42, 0.04);
+
+    /* Inputs */
+    --input-bg: #ffffff;
+    --input-border: rgba(15, 23, 42, 0.16);
+    --focus: rgba(37, 99, 235, 0.22);
+}
+
+/* Optional: DARK theme override (enable by adding class "theme-dark" to body or html) */
+.theme-dark {
+    --bg: #0f111a;
+    --card-bg: #1a1d29;
+    --card-bg-pastel: #1a1d29;
+    --border-color: #2d3245;
+    --sidebar-bg: #1a1d29;
+
+    --text-primary: #e2e8f0;
+    --text-secondary: #94a3b8;
+    --text-dim: rgba(255,255,255,0.52);
+
+    --accent-primary: #3b82f6;
+    --accent-success: #10b981;
+    --accent-warning: #f59e0b;
+    --accent-danger: #ef4444;
+
+    --surface-header: rgba(255, 255, 255, 0.03);
+    --surface-hover: rgba(255, 255, 255, 0.04);
+
+    --input-bg: #0f111a;
+    --input-border: #2d3245;
+    --focus: rgba(59, 130, 246, 0.20);
+}
 
 body {
-    background-color: var(--bg-dark);
+    background-color: var(--bg);
     color: var(--text-primary);
     font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
     line-height: 1.6;
 }
 
 /* Layout & Navigation */
 .navbar {
-    background-color: var(--card-bg) !important;
+    background-color: var(--card-bg) !important;
     border-bottom: 1px solid var(--border-color);
     padding: 0.75rem 1rem;
 }
 
 .navbar-brand {
     font-weight: 700;
     letter-spacing: -0.025em;
     color: var(--accent-primary) !important;
 }
 
 .nav-link {
     color: var(--text-secondary) !important;
     font-weight: 500;
     padding: 0.5rem 1rem !important;
     transition: all 0.2s;
 }
 
 .nav-link:hover, .nav-link.active {
     color: var(--text-primary) !important;
 }
 
 /* Cards & Containers */
 .card {
-    background-color: var(--card-bg);
+    background-color: var(--card-bg);
     border: 1px solid var(--border-color);
     border-radius: 0.75rem;
-    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
+    box-shadow: 0 10px 18px -18px rgba(15, 23, 42, 0.35);
     margin-bottom: 1.5rem;
 }
 
 .card-header {
     background-color: transparent;
     border-bottom: 1px solid var(--border-color);
     padding: 1.25rem;
     font-weight: 600;
 }
 
 .card-body {
     padding: 1.25rem;
 }
 
 .card-title {
     color: var(--text-primary);
     font-size: 1.1rem;
     margin-bottom: 1rem;
 }
 
+/* Pastel variant for sections (optional class on cards) */
+.card.pastel {
+    background: var(--card-bg-pastel);
+}
+
 /* Forms */
 .form-label {
-    color: var(--text-secondary);
+    color: var(--text-secondary);
     font-weight: 500;
     margin-bottom: 0.5rem;
 }
 
 .form-control, .form-select {
-    background-color: #0f111a;
-    border: 1px solid var(--border-color);
+    background-color: var(--input-bg);
+    border: 1px solid var(--input-border);
     color: var(--text-primary);
     border-radius: 0.5rem;
     padding: 0.625rem 0.875rem;
 }
 
 .form-control:focus, .form-select:focus {
-    background-color: #0f111a;
+    background-color: var(--input-bg);
     border-color: var(--accent-primary);
     color: var(--text-primary);
-    box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.2);
+    box-shadow: 0 0 0 3px var(--focus);
 }
 
 .form-text {
     color: var(--text-secondary);
 }
 
+/* Placeholder visibility (especially important on light theme) */
+.form-control::placeholder,
+textarea.form-control::placeholder {
+    color: var(--text-dim);
+    opacity: 1;
+}
+
 /* Buttons */
 .btn {
     border-radius: 0.5rem;
     padding: 0.625rem 1.25rem;
     font-weight: 500;
     transition: all 0.2s;
 }
 
 .btn-primary {
     background-color: var(--accent-primary);
     border-color: var(--accent-primary);
 }
 
 .btn-primary:hover {
-    background-color: #2563eb;
-    border-color: #2563eb;
+    filter: brightness(0.95);
     transform: translateY(-1px);
 }
 
 .btn-outline-secondary {
-    border-color: var(--border-color);
+    border-color: var(--border-color);
     color: var(--text-secondary);
 }
 
 .btn-outline-secondary:hover {
-    background-color: var(--border-color);
+    background-color: var(--surface-hover);
     color: var(--text-primary);
 }
 
 /* Tables */
 .table {
     color: var(--text-primary);
     border-color: var(--border-color);
 }
 
 .table thead th {
-    background-color: rgba(255, 255, 255, 0.02);
-    color: var(--text-secondary);
+    background-color: var(--surface-header);
+    color: var(--text-secondary);
     font-weight: 600;
     text-transform: uppercase;
     font-size: 0.75rem;
     letter-spacing: 0.05em;
     border-bottom: 1px solid var(--border-color);
 }
 
 .table-hover tbody tr:hover {
-    background-color: rgba(255, 255, 255, 0.02);
+    background-color: var(--surface-hover);
 }
 
 /* Badges & Status */
 .badge {
     padding: 0.4em 0.8em;
     border-radius: 2rem;
     font-weight: 600;
+    background: rgba(15, 23, 42, 0.06);
+    border: 1px solid rgba(15, 23, 42, 0.10);
 }
 
 .status-active { color: var(--accent-success); }
 .status-inactive { color: var(--text-secondary); }
 .status-error { color: var(--accent-danger); }
@@ -108,12 +173,22 @@
 .text-muted {
     color: var(--text-secondary) !important;
 }
 
 pre {
-    background-color: #0f111a;
-    border: 1px solid var(--border-color);
+    background-color: var(--input-bg);
+    border: 1px solid var(--border-color);
     padding: 1rem;
     border-radius: 0.5rem;
     color: #a5b4fc;
 }
+
+/* Links - readable and "SaaS-like" */
+a {
+    color: var(--accent-primary);
+    text-decoration: none;
+}
+a:hover {
+    text-decoration: underline;
+}

Что ты получишь после патча

Белый фон (--bg: #f6f7fb)

Чёрный/тёмный текст (читается без напряжения)

Пастельные карточки через класс .card.pastel (где нужно)

Формы/таблицы/бейджи выглядят аккуратно и “как на бирже проектов”

2) Задача для IDE (готовый тикет)
Название

UI: Перевести админ-панель на светлую тему (white + pastel cards) и привести типографику к читаемому виду

Цель

Сделать интерфейс в стиле “биржи проектов”:

белый фон

тёмный читаемый шрифт

пастельные карточки

аккуратные таблицы и формы

Что сделать

В admin.css:

заменить текущие переменные темы на светлые токены (как в патче)

добавить поддержку theme-dark как опциональный override

Привести компоненты к новым токенам:

body использует --bg, --text-primary

.card — белая поверхность + мягкая тень

добавить .card.pastel для секций (фон --card-bg-pastel)

формы:

инпуты --input-bg + --input-border

нормальный focus box-shadow: 0 0 0 3px var(--focus)

placeholder --text-dim

таблицы:

thead th — --surface-header

hover строк — --surface-hover

бейджи:

добавить лёгкий фон и рамку, чтобы читались на белом

ссылки:

цвет --accent-primary, underline на hover

Быстрая QA-проверка на 3 страницах:

“Заказ” (карточки/таблица этапов/ссылки)

“Провайдеры” (формы/подсказки/селекты)

“Продукты” (таблицы и кнопки)

Acceptance Criteria

 Белый фон и тёмный текст читаемы везде

 Карточки выглядят пастельно/мягко, без “грязного серого”

 Поля ввода и плейсхолдеры видны

 Таблицы читаемы, заголовки и hover заметны

 Ничего не “слепит” и не выглядит как дефолтный Bootstrap без стиля

3) Маленькая подсказка внедрения пастельных карточек

Где хочешь пастель — просто добавляйте класс:

<div class="card pastel">...</div>

🎨 Пастельные схемы карточек (готово к продакшену)

Все схемы спокойные, не кричащие, хорошо читаются на белом фоне и подходят для админки.

1️⃣ Голубая (информация / настройки / нейтральные блоки)

Использовать для:

информации о заказе

конфигурации LLM

параметров, справки, read-only данных

CSS
.card.pastel-blue {
    background: #f2f6ff;
    border-color: rgba(37, 99, 235, 0.18);
}

.card.pastel-blue .card-title {
    color: #1e3a8a;
}

.card.pastel-blue .muted,
.card.pastel-blue .text-muted {
    color: rgba(30, 58, 138, 0.65);
}

Визуально

аккуратный “информационный” блок

не отвлекает

идеально для шапок и summary

2️⃣ Зелёная (успех / активное / работает)

Использовать для:

активных ключей

успешных этапов

“всё работает”

CSS
.card.pastel-green {
    background: #ecfdf5;
    border-color: rgba(22, 163, 74, 0.22);
}

.card.pastel-green .card-title {
    color: #065f46;
}

.card.pastel-green .muted,
.card.pastel-green .text-muted {
    color: rgba(6, 95, 70, 0.65);
}

Визуально

ощущение стабильности

“всё ок” без ярко-зелёного

3️⃣ Фиолетовая (важное / внимание / AI / процессы)

Использовать для:

AI / LLM блоков

этапов обработки

системных процессов

CSS
.card.pastel-violet {
    background: #f5f3ff;
    border-color: rgba(124, 58, 237, 0.22);
}

.card.pastel-violet .card-title {
    color: #5b21b6;
}

.card.pastel-violet .muted,
.card.pastel-violet .text-muted {
    color: rgba(91, 33, 182, 0.65);
}

Визуально

ассоциация с AI / технологиями

“умный” акцент, не кислотный

🧩 Как применять в HTML (очень просто)
<div class="card pastel-blue">
  <div class="card-body">
    <h5 class="card-title">Информация о заказе</h5>
    ...
  </div>
</div>

<div class="card pastel-green">
  <div class="card-body">
    <h5 class="card-title">API ключ активен</h5>
    ...
  </div>
</div>

<div class="card pastel-violet">
  <div class="card-body">
    <h5 class="card-title">Конфигурация LLM</h5>
    ...
  </div>
</div>

📐 Общие правила (чтобы не было зоопарка)

❗ Очень важно, скажи это IDE:

Не больше 1 пастельного цвета на экран

Внутри одной страницы:

1 главный акцент (например, голубой)

остальные карточки — обычные белые

Таблицы НЕ красить пастелью, только карточки

Формы — всегда белые, даже внутри пастельных карточек

📝 Задача для IDE (финальная, чёткая)
Название

UI: Внедрить светлую SaaS-тему с пастельными карточками (blue / green / violet)

Цель

Сделать админ-панель визуально похожей на современные биржи и SaaS:

белый фон

тёмный читаемый текст

пастельные карточки для смысловых блоков

Что сделать

Добавить CSS-классы:

.card.pastel-blue

.card.pastel-green

.card.pastel-violet

Использовать пастельные карточки:

заказ / summary → pastel-blue

успешные статусы / активные ключи → pastel-green

AI / LLM / этапы обработки → pastel-violet

Проверить читаемость:

заголовки карточек контрастны

вторичный текст мягкий, но читаемый

нет “серого на сером”

Acceptance Criteria

 Интерфейс светлый, без тёмных фоновых блоков

 Текст читается без напряжения

 Пастель используется аккуратно, не пёстро

 Админка выглядит как продукт, а не dev-tool

🎯 Итог

После этого:

админка не устаёт глаза

выглядит дорого и спокойно

подходит и для тебя, и для любого менеджера/админа

визуально близка к примеру “Биржа проектов”, который ты показал

Если хочешь — следующим шагом могу:

сделать цветовую карту всего интерфейса (page → color)

или помочь IDE распределить пастель по конкретным страницам (заказы / провайдеры / продукты).