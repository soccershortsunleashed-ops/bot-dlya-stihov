1) ПАТЧ (unified diff) для admin.css

Применение: замени/вставь блоки точно как в diff. Это можно применить вручную или через git apply.

--- a/admin.css
+++ b/admin.css
@@ -1,17 +1,35 @@
 /* Admin SaaS Design System - Dark Theme */
 :root {
     --bg-dark: #0f111a;
     --card-bg: #1a1d29;
     --border-color: #2d3245;
     --text-primary: #e2e8f0;
     --text-secondary: #94a3b8;
     --accent-primary: #3b82f6;
     --accent-success: #10b981;
     --accent-warning: #f59e0b;
     --accent-danger: #ef4444;
     --sidebar-bg: #1a1d29;
+
+    /* Readability tokens (NEW): separation between label / helper / placeholder */
+    --text-muted: rgba(255, 255, 255, 0.72);
+    --text-dim: rgba(255, 255, 255, 0.52);
+    --text-faint: rgba(255, 255, 255, 0.40);
+
+    /* Hover/active surfaces */
+    --surface-hover: rgba(255, 255, 255, 0.04);
+    --surface-header: rgba(255, 255, 255, 0.03);
 }
 
 body {
     background-color: var(--bg-dark);
     color: var(--text-primary);
     font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
     line-height: 1.6;
 }
 
 /* Layout & Navigation */
 .navbar {
     background-color: var(--card-bg) !important;
     border-bottom: 1px solid var(--border-color);
     padding: 0.75rem 1rem;
 }
 
 .navbar-brand {
     font-weight: 700;
     letter-spacing: -0.025em;
     color: var(--accent-primary) !important;
 }
 
 .nav-link {
-    color: var(--text-secondary) !important;
+    /* Nav items must be readable in dark header */
+    color: var(--text-muted) !important;
     font-weight: 500;
     padding: 0.5rem 1rem !important;
     transition: all 0.2s;
 }
 
 .nav-link:hover, .nav-link.active {
     color: var(--text-primary) !important;
 }
 
 /* Cards & Containers */
 .card {
     background-color: var(--card-bg);
     border: 1px solid var(--border-color);
     border-radius: 0.75rem;
     box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
     margin-bottom: 1.5rem;
 }
 
 .card-header {
     background-color: transparent;
     border-bottom: 1px solid var(--border-color);
     padding: 1.25rem;
     font-weight: 600;
 }
 
 .card-body {
     padding: 1.25rem;
 }
 
 .card-title {
     color: var(--text-primary);
     font-size: 1.1rem;
     margin-bottom: 1rem;
 }
 
 /* Forms */
 .form-label {
-    color: var(--text-secondary);
+    /* Labels should not be "muted": they are primary anchors for readability */
+    color: var(--text-primary);
     font-weight: 500;
     margin-bottom: 0.5rem;
 }
 
 .form-control, .form-select {
     background-color: #0f111a;
     border: 1px solid var(--border-color);
     color: var(--text-primary);
     border-radius: 0.5rem;
     padding: 0.625rem 0.875rem;
 }
 
+.form-control::placeholder,
+.form-select::placeholder,
+textarea.form-control::placeholder {
+    /* Placeholder must be visible on dark inputs */
+    color: var(--text-dim);
+    opacity: 1;
+}
+
 .form-control:focus, .form-select:focus {
     background-color: #0f111a;
     border-color: var(--accent-primary);
     color: var(--text-primary);
     box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.2);
 }
 
 .form-text {
-    color: var(--text-secondary);
+    /* Helper text should be softer than primary, but still readable */
+    color: var(--text-muted);
 }
 
+/* Disabled fields should still be readable (not "grey on grey") */
+.form-control:disabled, .form-select:disabled, textarea:disabled {
+    color: var(--text-dim);
+    opacity: 1;
+    cursor: not-allowed;
+}
+
 /* Buttons */
 .btn {
     border-radius: 0.5rem;
     padding: 0.625rem 1.25rem;
     font-weight: 500;
     transition: all 0.2s;
 }
@@ -71,25 +89,41 @@
     color: var(--text-primary);
 }
 
 /* Tables */
 .table {
     color: var(--text-primary);
     border-color: var(--border-color);
 }
 
 .table thead th {
-    background-color: rgba(255, 255, 255, 0.02);
-    color: var(--text-secondary);
+    background-color: var(--surface-header);
+    /* Header text must be more readable than secondary */
+    color: var(--text-muted);
     font-weight: 600;
     text-transform: uppercase;
     font-size: 0.75rem;
     letter-spacing: 0.05em;
     border-bottom: 1px solid var(--border-color);
 }
 
+.table tbody td {
+    color: var(--text-primary);
+}
+
 .table-hover tbody tr:hover {
-    background-color: rgba(255, 255, 255, 0.02);
+    background-color: var(--surface-hover);
 }
 
 /* Badges & Status */
 .badge {
     padding: 0.4em 0.8em;
     border-radius: 2rem;
     font-weight: 600;
+    /* Make badges readable on dark surfaces */
+    background: rgba(255, 255, 255, 0.04);
+    border: 1px solid rgba(255, 255, 255, 0.08);
 }
 
 .status-active { color: var(--accent-success); }
-.status-inactive { color: var(--text-secondary); }
+.status-inactive { color: var(--text-muted); }
 .status-error { color: var(--accent-danger); }
 
 /* Custom Components */
 .api-key-mask {
     font-family: monospace;
     letter-spacing: 0.1em;
 }
@@ -108,14 +142,40 @@
 /* Utilities */
 .text-muted {
-    color: var(--text-secondary) !important;
+    /* Bootstrap-like utility should be readable in dark theme */
+    color: var(--text-muted) !important;
 }
 
 pre {
     background-color: #0f111a;
     border: 1px solid var(--border-color);
     padding: 1rem;
     border-radius: 0.5rem;
     color: #a5b4fc;
 }
 
+/* Readable empty state (e.g. "Параметры не заданы") */
+.empty-state {
+    text-align: center;
+    color: var(--text-muted);
+}
+.empty-state .title {
+    color: var(--text-primary);
+    font-weight: 600;
+}
+.empty-state .description {
+    color: var(--text-muted);
+}
+
 /* Animations */
 @keyframes fadeIn {
     from { opacity: 0; transform: translateY(10px); }
     to { opacity: 1; transform: translateY(0); }
 }
 
 .animate-fade-in {
     animation: fadeIn 0.3s ease-out forwards;
 }

Что этот патч реально исправляет

Лейблы форм становятся контрастными (теперь их видно).

Placeholder/хелперы появляются и читаются.

Заголовки таблиц перестают быть “серым шумом”.

.text-muted больше не превращает половину интерфейса в невидимый текст.

Hover строк таблицы и badges становятся заметными на тёмном фоне.