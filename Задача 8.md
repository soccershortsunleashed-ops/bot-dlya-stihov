Сделаем 🔥 “Точнее → Креативнее” как в нормальном SaaS: видимая дорожка, градиент от серого к синему, аккуратный прогресс, без разливов и артефактов. Ниже — полная переписанная правка только для этого элемента (CSS + 10 строк JS).

1) УДАЛИТЬ старую правку (важно)

Убери любые старые стили ползунка, особенно если есть:

box-shadow: -1000px ... (он и “разливал” цвет)

::-moz-range-progress (можно оставить, но теперь будет единый способ)

.creative-slider { background: ... } если он мешает

2) ВСТАВИТЬ новый CSS (полностью)

Вставь в конец admin.css, чтобы он переопределил всё старое.

/* =========================
   SaaS Range Slider (Accuracy → Creativity)
   - no bleeding
   - visible track
   - gradient from grey to blue
   ========================= */

:root {
  /* Можно подстроить под дизайн */
  --range-height: 8px;
  --range-thumb: 16px;

  /* "серый" для точности + "синий" для креативности */
  --range-grey: rgba(15, 23, 42, 0.28);
  --range-grey-weak: rgba(15, 23, 42, 0.18);
  --range-blue: rgba(37, 99, 235, 0.90);
  --range-blue-soft: rgba(37, 99, 235, 0.35);
}

/* Базовый слайдер */
input[type="range"].creative-range {
  -webkit-appearance: none;
  appearance: none;
  width: 100%;
  height: var(--range-height);
  border-radius: 999px;
  cursor: pointer;
  outline: none;

  /* Вся дорожка = градиент серый -> синий (направление соответствует "Точнее -> Креативнее") */
  background: linear-gradient(to right, var(--range-grey) 0%, var(--range-blue) 100%);
  position: relative;
}

/* Прогресс-оверлей до бегунка (тонкая подсветка) — рисуем ПОВЕРХ градиента через background-size */
input[type="range"].creative-range {
  background-image:
    linear-gradient(to right, rgba(255,255,255,0.65) 0%, rgba(255,255,255,0.65) 100%),
    linear-gradient(to right, var(--range-grey) 0%, var(--range-blue) 100%);
  /* 1й слой — беловатая подсветка прогресса, 2й слой — базовый градиент */
  background-repeat: no-repeat, no-repeat;

  /* ВАЖНО: первый слой будет иметь ширину прогресса (меняется JS) */
  background-size: var(--range-progress, 50%) 100%, 100% 100%;
  background-position: 0 0, 0 0;

  /* Немного “вдавленности”, чтобы дорожка читалась на пастельной карточке */
  box-shadow: inset 0 0 0 1px rgba(15, 23, 42, 0.10);
}

/* WebKit track reset (чтобы не конфликтовал с background на input) */
input[type="range"].creative-range::-webkit-slider-runnable-track {
  height: var(--range-height);
  background: transparent;
  border-radius: 999px;
}

/* Firefox track reset */
input[type="range"].creative-range::-moz-range-track {
  height: var(--range-height);
  background: transparent;
  border-radius: 999px;
}

/* Thumb (бегунок) */
input[type="range"].creative-range::-webkit-slider-thumb {
  -webkit-appearance: none;
  appearance: none;
  width: var(--range-thumb);
  height: var(--range-thumb);
  border-radius: 50%;
  background: #ffffff;
  border: 3px solid rgba(37, 99, 235, 0.95);
  box-shadow: 0 2px 8px rgba(15, 23, 42, 0.22);
  margin-top: calc((var(--range-height) - var(--range-thumb)) / 2);
}

input[type="range"].creative-range::-moz-range-thumb {
  width: var(--range-thumb);
  height: var(--range-thumb);
  border-radius: 50%;
  background: #ffffff;
  border: 3px solid rgba(37, 99, 235, 0.95);
  box-shadow: 0 2px 8px rgba(15, 23, 42, 0.22);
}

/* Hover / active — легкий “живой” эффект */
input[type="range"].creative-range:hover {
  box-shadow: inset 0 0 0 1px rgba(15, 23, 42, 0.14);
}

input[type="range"].creative-range:active::-webkit-slider-thumb {
  transform: scale(1.05);
}

input[type="range"].creative-range:active::-moz-range-thumb {
  transform: scale(1.05);
}

/* Focus (доступность) */
input[type="range"].creative-range:focus-visible::-webkit-slider-thumb {
  box-shadow:
    0 0 0 5px rgba(37, 99, 235, 0.18),
    0 2px 8px rgba(15, 23, 42, 0.22);
}

input[type="range"].creative-range:focus-visible::-moz-range-thumb {
  box-shadow:
    0 0 0 5px rgba(37, 99, 235, 0.18),
    0 2px 8px rgba(15, 23, 42, 0.22);
}

3) ВСТАВИТЬ JS (обновляет прогресс без артефактов)

Это можно положить в ваш общий admin.js, или прямо на страницу.

document.querySelectorAll('input[type="range"].creative-range').forEach((slider) => {
  const update = () => {
    const min = Number(slider.min || 0);
    const max = Number(slider.max || 1);
    const val = Number(slider.value || 0);
    const percent = ((val - min) / (max - min)) * 100;
    slider.style.setProperty('--range-progress', `${percent}%`);
  };

  slider.addEventListener('input', update);
  slider.addEventListener('change', update);
  update();
});

4) Что поменять в HTML (1 строка)

Чтобы новые стили применились, добавь класс creative-range:

<input type="range" class="creative-range" min="0" max="1" step="0.1" value="0.7">

Что ты получишь

Полоса точно видна всегда

Оттенок слева серее (“Точнее”), справа синее (“Креативнее”)

До бегунка — аккуратная подсветка прогресса

Никаких “разливов” по экрану (мы больше не используем хак с огромной тенью)

Задача для IDE (коротко)

UI: Переписать ползунок “Креативность” на градиентную шкалу “Точнее → Креативнее”

Удалить старый range CSS с любыми box-shadow:-1000px...

Добавить новый CSS для .creative-range (градиент серый→синий, подсветка прогресса через background-size)

Добавить JS-обновление --range-progress

Проверить: Chrome/Firefox, пастельная карточка, масштаб 100%/125%