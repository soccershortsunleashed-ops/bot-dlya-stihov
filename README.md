# Бот для написания стихов

Профессиональный Telegram-бот для генерации стихотворений с использованием искусственного интеллекта (Gemini, OpenAI, YandexGPT) и интеграцией платежной системы ЮKassa.

## 🚀 Возможности

- **Генерация стихов**: Использование передовых LLM (Gemini, OpenAI, YandexGPT) для создания уникального контента.
- **Мультипровайдерность**: Гибкая настройка AI-провайдеров через админ-панель.
- **Платежи**: Интеграция с ЮKassa для продажи тарифных планов.
- **Админ-панель**: Веб-интерфейс на FastAPI для управления заказами, пользователями и конфигурациями провайдеров.
- **Асинхронность**: Обработка тяжелых задач (генерация, озвучка) через Celery + Redis.
- **S3 Хранилище**: Сохранение артефактов (тексты, аудио) в S3-совместимое хранилище.

## 🏗 Архитектура проекта

Проект построен с использованием принципов Clean Architecture и разделен на слои:

```text
app/
├── application/       # Use Cases и бизнес-логика приложения
│   ├── dto/           # Объекты передачи данных (Pydantic схемы)
│   ├── services/      # Внутренние сервисы (политика контента, сборка промптов)
│   └── use_cases/     # Сценарии использования (создание заказа, оплата)
├── domain/            # Доменные модели и перечисления (Enums)
├── infra/             # Инфраструктурный слой (реализация интерфейсов)
│   ├── ai/            # Провайдеры ИИ (Gemini, OpenAI, YandexGPT)
│   ├── config/        # Настройки приложения и логирование
│   ├── db/            # SQLAlchemy модели, репозитории и миграции Alembic
│   ├── payments/      # Интеграция с платежными шлюзами (ЮKassa)
│   ├── queue/         # Celery задачи и конфигурация
│   └── storage/       # Работа с S3 хранилищем
├── bot/               # Слой Telegram бота (Aiogram 3.x)
│   ├── fsm/           # Состояния (Finite State Machine)
│   ├── keyboards/     # Клавиатуры бота
│   ├── routers/       # Обработчики команд и сообщений
│   └── texts/         # Локализация текстов
└── web/               # Слой веб-интерфейса (FastAPI + Jinja2)
    ├── routes/        # Эндпоинты админ-панели и вебхуки
    ├── templates/     # HTML шаблоны
    └── static/        # CSS и статические файлы
```

## 🛠 Технологический стек

- **Language**: Python 3.10+
- **Bot Framework**: Aiogram 3.x
- **Web Framework**: FastAPI
- **Database**: PostgreSQL (SQLAlchemy + Alembic)
- **Task Queue**: Celery + Redis
- **AI Integration**: Google Gemini, OpenAI API, Yandex GPT, Yandex SpeechKit
- **Payments**: ЮKassa API
- **DevOps**: Docker, Docker Compose

## 🔧 Установка и запуск

1. **Клонируйте репозиторий**:
   ```bash
   git clone https://github.com/soccershortsunleashed-ops/bot-dlya-stihov.git
   cd bot-dlya-stihov
   ```

2. **Настройте окружение**:
   Скопируйте `.env.example` в `.env` и заполните необходимые переменные (API токены, данные БД).

3. **Запуск через Docker**:
   ```bash
   docker-compose up -d --build
   ```

4. **Локальный запуск (без Docker)**:
   - Установите зависимости: `pip install poetry && poetry install`
   - Запустите бота: `run_bot.bat` (или `python -m app.bot.main`)
   - Запустите веб-панель: `run_web.bat` (или `python -m app.web.main`)
   - Запустите воркер: `run_worker.bat` (или `celery -A app.infra.queue.celery_app worker`)

## 📝 Лицензия

Проект распространяется на условиях собственной лицензии.